port: 7880

rtc:
  tcp_port: 7881
  udp_port: 7882
  use_external_ip: false
  port_range_start: 50000
  port_range_end: 50100

redis:
  address: redis:6379

webhook:
  api_key: devkey
  urls:
    # `server` service on the compose network.
    - http://server:3002/api/livekit/webhook

turn:
  enabled: false
  # For staging/prod, point at coturn (set domain / tls_port / udp_port).

log_level: info
