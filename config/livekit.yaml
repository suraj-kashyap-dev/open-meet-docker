port: 7880

rtc:
  tcp_port: 7881
  udp_port: 7882
  use_external_ip: false
  port_range_start: 50000
  port_range_end: 50100

redis:
  address: redis:6379

webhook:
  api_key: devkey
  urls:
    # The API runs as the `server` service on the compose network.
    - http://server:3002/api/livekit/webhook

turn:
  enabled: false
  # When promoting to staging/prod, point at coturn:
  # domain: turn.example.com
  # tls_port: 5349
  # udp_port: 3478

log_level: info
